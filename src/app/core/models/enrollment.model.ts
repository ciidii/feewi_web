/**
 * RE-EXPORT GLOBAL (Transition strategy)
 * Toutes les définitions ont été segmentées dans le dossier ./enrollment/
 * pour une meilleure maintenabilité (Clean Architecture).
 */

export * from './enrollment/index';
